/**
 * Chrome icon set, curated from Phosphor "regular" weight.
 *
 * Phosphor sits between Heroicons (SaaS reflex) and Lucide (Feather
 * derivative): rounded line caps, friendly proportions, characterful
 * without being playful. Re-export the small set the chrome uses so
 * every chrome component reaches into one place, and so swapping the
 * family later happens here, not at call sites.
 */
import {
  House,
  GraduationCap,
  Flag,
  ShieldCheck,
  FolderSimple,
  GearSix,
  Buildings,
  Lifebuoy,
  MagnifyingGlass,
  CaretRight,
  List as Bars,
  X as Close,
  SignOut,
} from "@phosphor-icons/react";

export {
  House,
  GraduationCap,
  Flag,
  ShieldCheck,
  FolderSimple,
  GearSix,
  Buildings,
  Lifebuoy,
  MagnifyingGlass,
  CaretRight,
  Bars,
  Close,
  SignOut,
};
